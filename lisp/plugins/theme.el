;;; theme.el --- Theme configuration -*- lexical-binding: t; -*-

;;; Commentary:
;; Install and enable the Atom One Dark theme.

;;; Code:

(use-package atom-one-dark-theme
  :config
  (load-theme 'atom-one-dark t))

(provide 'theme)
;;; theme.el ends here

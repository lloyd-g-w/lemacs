;;; early-init.el --- Early initialization -*- lexical-binding: t; -*-

;;; Commentary:
;; Runs before init.el, before package.el and the UI are initialized.
;; Intentionally empty for now — fill in as the config grows.

;;; Code:

;;; early-init.el ends here
